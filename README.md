Template project using vite/react with MUI

To start
```
pnpm dev
```

---
Start json-server for local api
```
npx json-server --watch db.json --port 8000
```

Update the db.json file to add data for json-server